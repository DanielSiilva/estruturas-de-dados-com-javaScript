//Remover do fim do array

const listNumber = [1, 2, 3, 4]
listNumber.pop(3)
//console.log(listNumber);
listNumber.pop(2)
console.log(listNumber)

//Removendo do inicio do array => metodo shift

listNumber.shift()

console.log(listNumber);

//Os metodos shift e unshift permitem que um array emule uma estrutura de dados básica de fila (queue). 
